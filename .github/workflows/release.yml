name: Публикация релиза

on:
  release:
    types:
      - published
  workflow_dispatch:

permissions:
  contents: read

jobs:
  release:
    uses: autumn-library/workflows/.github/workflows/release.yml@main
    with:
      package_mask: "oneflag-*.ospx"
    secrets:
      PUSH_TOKEN: ${{ secrets.PUSH_TOKEN }}